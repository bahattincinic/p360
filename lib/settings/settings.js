// global settings for server side
console.log('settings');

var _Settings = function() {
    var self = this;
    self.roomTimeout = 1000 * 30;
    self.xx = self.roomTimeout / 1000;
    shuffleName: 'x00x';
}

Settings = new _Settings();
